# Component Extraction Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Extract repeated UI patterns across clipboard-list, settings, and image-preview into focused reusable components, and add two missing spartan helm primitives (checkbox, separator).

**Architecture:** Eight new components split across `src/app/shared/ui/` (app-wide) and `src/app/features/settings/components/` (settings-scoped). Spartan checkbox and separator are generated via the CLI first since SettingCheckboxComponent depends on HlmCheckbox. Consumer components are refactored last, one per task.

**Tech Stack:** Angular 21, signals (`input()` / `output()`), `ChangeDetectionStrategy.OnPush`, spartan-ng/helm, ng-icons/lucide, Vitest, pnpm

---

## File Map

**Create:**
- `src/app/shared/ui/page-header/page-header.component.ts`
- `src/app/shared/ui/page-header/page-header.component.spec.ts`
- `src/app/shared/ui/empty-state/empty-state.component.ts`
- `src/app/shared/ui/empty-state/empty-state.component.spec.ts`
- `src/app/shared/ui/keyboard-hint/keyboard-hint.component.ts`
- `src/app/shared/ui/keyboard-hint/keyboard-hint.component.spec.ts`
- `src/app/shared/ui/loading-spinner/loading-spinner.component.ts`
- `src/app/shared/ui/loading-spinner/loading-spinner.component.spec.ts`
- `src/app/features/settings/components/setting-field/setting-field.component.ts`
- `src/app/features/settings/components/setting-field/setting-field.component.spec.ts`
- `src/app/features/settings/components/setting-checkbox/setting-checkbox.component.ts`
- `src/app/features/settings/components/setting-checkbox/setting-checkbox.component.spec.ts`
- `src/libs/ui/checkbox/` (generated by spartan CLI)
- `src/libs/ui/separator/` (generated by spartan CLI)

**Modify:**
- `tsconfig.json` — spartan CLI adds path aliases for checkbox and separator
- `src/app/features/clipboard-list/clipboard-list.component.ts` — use PageHeader, EmptyState, KeyboardHint
- `src/app/features/settings/settings.component.ts` — use PageHeader, LoadingSpinner, HlmSeparator, SettingField, SettingCheckbox; simplify event handlers
- `src/app/features/image-preview/image-preview.component.ts` — use PageHeader, LoadingSpinner, EmptyState

---

## Task 1: Generate spartan checkbox and separator

**Files:**
- Modify: `tsconfig.json` (CLI updates automatically)
- Create: `src/libs/ui/checkbox/` (CLI generates)
- Create: `src/libs/ui/separator/` (CLI generates)

- [ ] **Step 1: Generate checkbox**

```bash
pnpm spartan add checkbox
```

When prompted for directory, accept the default (`src/libs/ui`).

- [ ] **Step 2: Generate separator**

```bash
pnpm spartan add separator
```

- [ ] **Step 3: Verify tsconfig paths were added**

Open `tsconfig.json`. Confirm both entries are present under `"paths"`:
```json
"@spartan-ng/helm/checkbox": ["./src/libs/ui/checkbox/src/index.ts"],
"@spartan-ng/helm/separator": ["./src/libs/ui/separator/src/index.ts"]
```

If the CLI did not add them automatically, add them manually.

- [ ] **Step 4: Inspect generated HlmCheckbox API**

Read `src/libs/ui/checkbox/src/index.ts` and the component file it exports. Note the exact selector, input names (`checked`, `disabled`), and output name (`checkedChange`). The plan uses `hlm-checkbox` as selector and `(checkedChange)` emitting `boolean` — adjust Task 7 if the generated API differs.

- [ ] **Step 5: Inspect generated HlmSeparator**

Read `src/libs/ui/separator/src/index.ts`. Note the selector (typically `hlm-separator` or a directive applied to `brn-separator`).

- [ ] **Step 6: Commit**

```bash
git add src/libs/ui/checkbox src/libs/ui/separator tsconfig.json
git commit -m "feat(ui): add spartan checkbox and separator helm components"
```

---

## Task 2: PageHeaderComponent

**Files:**
- Create: `src/app/shared/ui/page-header/page-header.component.spec.ts`
- Create: `src/app/shared/ui/page-header/page-header.component.ts`

- [ ] **Step 1: Write the failing test**

Create `src/app/shared/ui/page-header/page-header.component.spec.ts`:

```typescript
import { PageHeaderComponent } from './page-header.component';

describe('PageHeaderComponent', () => {
  it('is defined', () => {
    expect(PageHeaderComponent).toBeDefined();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
pnpm test
```

Expected: FAIL — `Cannot find module './page-header.component'`

- [ ] **Step 3: Implement PageHeaderComponent**

Create `src/app/shared/ui/page-header/page-header.component.ts`:

```typescript
import { ChangeDetectionStrategy, Component, input } from '@angular/core';

@Component({
  selector: 'app-page-header',
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: `
    <div
      class="px-3.5 h-11 flex items-center justify-between shrink-0 border-b"
      [class]="variant() === 'dark' ? 'bg-zinc-900 border-zinc-800' : 'bg-card border-border'"
      [attr.data-tauri-drag-region]="dragRegion() ? '' : null">
      <div class="flex items-center gap-2">
        <ng-content select="[start]" />
      </div>
      <div class="flex items-center gap-2">
        <ng-content select="[end]" />
      </div>
    </div>
  `,
})
export class PageHeaderComponent {
  variant = input<'default' | 'dark'>('default');
  dragRegion = input(true);
}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
pnpm test
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/app/shared/ui/page-header
git commit -m "feat(ui): add PageHeaderComponent"
```

---

## Task 3: EmptyStateComponent

**Files:**
- Create: `src/app/shared/ui/empty-state/empty-state.component.spec.ts`
- Create: `src/app/shared/ui/empty-state/empty-state.component.ts`

- [ ] **Step 1: Write the failing test**

Create `src/app/shared/ui/empty-state/empty-state.component.spec.ts`:

```typescript
import { EmptyStateComponent } from './empty-state.component';

describe('EmptyStateComponent', () => {
  it('is defined', () => {
    expect(EmptyStateComponent).toBeDefined();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
pnpm test
```

Expected: FAIL — `Cannot find module './empty-state.component'`

- [ ] **Step 3: Implement EmptyStateComponent**

Create `src/app/shared/ui/empty-state/empty-state.component.ts`:

```typescript
import { ChangeDetectionStrategy, Component, computed, input } from '@angular/core';
import { NgIcon, provideIcons } from '@ng-icons/core';
import {
  lucideAlertCircle,
  lucideBookmark,
  lucideClipboard,
} from '@ng-icons/lucide';
import { HlmIcon } from '@spartan-ng/helm/icon';

@Component({
  selector: 'app-empty-state',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [NgIcon, HlmIcon],
  providers: [provideIcons({ lucideAlertCircle, lucideBookmark, lucideClipboard })],
  template: `
    <div class="flex flex-col items-center justify-center h-full py-10 text-center gap-3">
      <div [class]="iconContainerClass()">
        <ng-icon hlm [size]="variant() === 'destructive' ? 'sm' : 'base'" [name]="icon()" [class]="iconClass()" />
      </div>
      <p class="text-[13px] text-muted-foreground">{{ title() }}</p>
      @if (hint()) {
        <p class="text-[11px] text-muted-foreground">{{ hint() }}</p>
      }
      <ng-content />
    </div>
  `,
})
export class EmptyStateComponent {
  icon = input.required<string>();
  title = input.required<string>();
  hint = input<string>();
  variant = input<'default' | 'destructive'>('default');

  protected iconContainerClass = computed(() =>
    this.variant() === 'destructive'
      ? 'w-9 h-9 rounded-full bg-red-500/10 flex items-center justify-center'
      : 'w-10 h-10 rounded-xl bg-card flex items-center justify-center'
  );

  protected iconClass = computed(() =>
    this.variant() === 'destructive' ? 'text-red-400' : 'text-muted-foreground'
  );
}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
pnpm test
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/app/shared/ui/empty-state
git commit -m "feat(ui): add EmptyStateComponent"
```

---

## Task 4: KeyboardHintComponent

**Files:**
- Create: `src/app/shared/ui/keyboard-hint/keyboard-hint.component.spec.ts`
- Create: `src/app/shared/ui/keyboard-hint/keyboard-hint.component.ts`

- [ ] **Step 1: Write the failing test**

Create `src/app/shared/ui/keyboard-hint/keyboard-hint.component.spec.ts`:

```typescript
import { KeyboardHintComponent } from './keyboard-hint.component';

describe('KeyboardHintComponent', () => {
  it('is defined', () => {
    expect(KeyboardHintComponent).toBeDefined();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
pnpm test
```

Expected: FAIL — `Cannot find module './keyboard-hint.component'`

- [ ] **Step 3: Implement KeyboardHintComponent**

Create `src/app/shared/ui/keyboard-hint/keyboard-hint.component.ts`:

```typescript
import { ChangeDetectionStrategy, Component, input } from '@angular/core';

@Component({
  selector: 'app-keyboard-hint',
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: `
    <span class="flex items-center gap-1 text-[10px] text-muted-foreground whitespace-nowrap">
      <kbd class="inline-flex items-center px-1 py-0.5 bg-muted border border-border rounded text-[10px] font-mono text-muted-foreground leading-none">{{ key() }}</kbd>
      {{ label() }}
    </span>
  `,
})
export class KeyboardHintComponent {
  key = input.required<string>();
  label = input.required<string>();
}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
pnpm test
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/app/shared/ui/keyboard-hint
git commit -m "feat(ui): add KeyboardHintComponent"
```

---

## Task 5: LoadingSpinnerComponent

**Files:**
- Create: `src/app/shared/ui/loading-spinner/loading-spinner.component.spec.ts`
- Create: `src/app/shared/ui/loading-spinner/loading-spinner.component.ts`

- [ ] **Step 1: Write the failing test**

Create `src/app/shared/ui/loading-spinner/loading-spinner.component.spec.ts`:

```typescript
import { LoadingSpinnerComponent } from './loading-spinner.component';

describe('LoadingSpinnerComponent', () => {
  it('is defined', () => {
    expect(LoadingSpinnerComponent).toBeDefined();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
pnpm test
```

Expected: FAIL — `Cannot find module './loading-spinner.component'`

- [ ] **Step 3: Implement LoadingSpinnerComponent**

Create `src/app/shared/ui/loading-spinner/loading-spinner.component.ts`:

```typescript
import { ChangeDetectionStrategy, Component, computed, input } from '@angular/core';

@Component({
  selector: 'app-loading-spinner',
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: `
    <div class="w-5 h-5 border-2 rounded-full animate-spin" [class]="borderClass()"></div>
  `,
})
export class LoadingSpinnerComponent {
  variant = input<'default' | 'dark'>('default');

  protected borderClass = computed(() =>
    this.variant() === 'dark'
      ? 'border-zinc-800 border-t-zinc-500'
      : 'border-muted border-t-muted-foreground'
  );
}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
pnpm test
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/app/shared/ui/loading-spinner
git commit -m "feat(ui): add LoadingSpinnerComponent"
```

---

## Task 6: SettingFieldComponent

**Files:**
- Create: `src/app/features/settings/components/setting-field/setting-field.component.spec.ts`
- Create: `src/app/features/settings/components/setting-field/setting-field.component.ts`

- [ ] **Step 1: Write the failing test**

Create `src/app/features/settings/components/setting-field/setting-field.component.spec.ts`:

```typescript
import { SettingFieldComponent } from './setting-field.component';

describe('SettingFieldComponent', () => {
  it('is defined', () => {
    expect(SettingFieldComponent).toBeDefined();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
pnpm test
```

Expected: FAIL — `Cannot find module './setting-field.component'`

- [ ] **Step 3: Implement SettingFieldComponent**

Create `src/app/features/settings/components/setting-field/setting-field.component.ts`:

```typescript
import { ChangeDetectionStrategy, Component, input } from '@angular/core';
import { HlmLabel } from '@spartan-ng/helm/label';

@Component({
  selector: 'app-setting-field',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [HlmLabel],
  template: `
    <div class="space-y-1.5">
      @if (label()) {
        <label hlmLabel class="block uppercase tracking-wider">{{ label() }}</label>
      }
      <ng-content />
      @if (hint()) {
        <p class="text-[11px] text-muted-foreground">{{ hint() }}</p>
      }
    </div>
  `,
})
export class SettingFieldComponent {
  label = input<string>();
  hint = input<string>();
}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
pnpm test
```

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/app/features/settings/components/setting-field
git commit -m "feat(settings): add SettingFieldComponent"
```

---

## Task 7: SettingCheckboxComponent

**Files:**
- Create: `src/app/features/settings/components/setting-checkbox/setting-checkbox.component.spec.ts`
- Create: `src/app/features/settings/components/setting-checkbox/setting-checkbox.component.ts`

**Prerequisite:** Task 1 must be complete (HlmCheckbox generated and tsconfig path added).

- [ ] **Step 1: Confirm generated HlmCheckbox API**

Read the generated checkbox component (found during Task 1 Step 4). Confirm the selector, `checked` input, and `checkedChange` output. The implementation below assumes:
- Selector: `hlm-checkbox`
- Input: `[checked]="boolean"`
- Output: `(checkedChange)` emitting `boolean`

If the generated API differs, adjust Step 3 accordingly.

- [ ] **Step 2: Write the failing test**

Create `src/app/features/settings/components/setting-checkbox/setting-checkbox.component.spec.ts`:

```typescript
import { SettingCheckboxComponent } from './setting-checkbox.component';

describe('SettingCheckboxComponent', () => {
  it('is defined', () => {
    expect(SettingCheckboxComponent).toBeDefined();
  });
});
```

- [ ] **Step 3: Run test to verify it fails**

```bash
pnpm test
```

Expected: FAIL — `Cannot find module './setting-checkbox.component'`

- [ ] **Step 4: Implement SettingCheckboxComponent**

Create `src/app/features/settings/components/setting-checkbox/setting-checkbox.component.ts`:

```typescript
import { ChangeDetectionStrategy, Component, input, output } from '@angular/core';
import { HlmCheckboxImports } from '@spartan-ng/helm/checkbox';
import { HlmLabel } from '@spartan-ng/helm/label';

@Component({
  selector: 'app-setting-checkbox',
  changeDetection: ChangeDetectionStrategy.OnPush,
  imports: [HlmCheckboxImports, HlmLabel],
  template: `
    <div class="flex items-center gap-2">
      <hlm-checkbox
        [id]="id()"
        [checked]="checked()"
        (checkedChange)="checkedChange.emit($event)"
      />
      <label hlmLabel [for]="id()" class="uppercase tracking-wider cursor-pointer">{{ label() }}</label>
    </div>
  `,
})
export class SettingCheckboxComponent {
  id = input.required<string>();
  label = input.required<string>();
  checked = input.required<boolean>();
  checkedChange = output<boolean>();
}
```

> **Note:** The exact import name from `@spartan-ng/helm/checkbox` depends on what the CLI generated. Open `src/libs/ui/checkbox/src/index.ts` and use whatever is exported (commonly `HlmCheckboxComponent` or `HlmCheckboxImports`). Adjust the import and `imports: []` array to match.

- [ ] **Step 5: Run test to verify it passes**

```bash
pnpm test
```

Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/app/features/settings/components/setting-checkbox
git commit -m "feat(settings): add SettingCheckboxComponent"
```

---

## Task 8: Refactor clipboard-list

**Files:**
- Modify: `src/app/features/clipboard-list/clipboard-list.component.ts`

Replace the inline header, all empty/error state blocks, and the five `<kbd>` hint spans with the new shared components.

- [ ] **Step 1: Run tests to confirm green baseline**

```bash
pnpm test
```

Expected: all PASS

- [ ] **Step 2: Update imports and providers**

In `clipboard-list.component.ts`, update the `imports` array and `providers`:

```typescript
import { PageHeaderComponent } from '../../shared/ui/page-header/page-header.component';
import { EmptyStateComponent } from '../../shared/ui/empty-state/empty-state.component';
import { KeyboardHintComponent } from '../../shared/ui/keyboard-hint/keyboard-hint.component';
```

Add to the component `imports` array: `PageHeaderComponent, EmptyStateComponent, KeyboardHintComponent`

Remove from `providers`: `lucideAlertCircle, lucideBookmark` (now registered inside EmptyStateComponent). Keep: `lucideClipboard, lucideSettings, lucideSearch, lucideX`.

- [ ] **Step 3: Replace the header div**

Remove:
```html
<div class="px-3.5 h-11 flex items-center justify-between shrink-0 bg-card border-b border-border" data-tauri-drag-region>
  <div class="flex items-center gap-2">
    <ng-icon hlm size="sm" name="lucideClipboard" class="text-muted-foreground shrink-0" />
    <span class="text-[13px] font-semibold text-foreground tracking-tight">{{ 'CLIPBOARD.TITLE' | translate }}</span>
    @if (allEntries().length > 0) {
      <span hlmBadge variant="secondary">{{ allEntries().length }}</span>
    }
  </div>
  <a routerLink="/settings" class="p-1.5 rounded-md text-muted-foreground hover:text-foreground hover:bg-muted transition-colors">
    <ng-icon hlm size="sm" name="lucideSettings" />
  </a>
</div>
```

Replace with:
```html
<app-page-header>
  <ng-container start>
    <ng-icon hlm size="sm" name="lucideClipboard" class="text-muted-foreground shrink-0" />
    <span class="text-[13px] font-semibold text-foreground tracking-tight">{{ 'CLIPBOARD.TITLE' | translate }}</span>
    @if (allEntries().length > 0) {
      <span hlmBadge variant="secondary">{{ allEntries().length }}</span>
    }
  </ng-container>
  <ng-container end>
    <a routerLink="/settings" class="p-1.5 rounded-md text-muted-foreground hover:text-foreground hover:bg-muted transition-colors">
      <ng-icon hlm size="sm" name="lucideSettings" />
    </a>
  </ng-container>
</app-page-header>
```

- [ ] **Step 4: Replace error state**

Remove:
```html
<div class="flex flex-col items-center justify-center h-full py-10 text-center">
  <div class="w-9 h-9 rounded-full bg-red-500/10 flex items-center justify-center mb-3">
    <ng-icon hlm size="sm" name="lucideAlertCircle" class="text-red-400" />
  </div>
  <p class="text-[13px] text-muted-foreground mb-1.5">{{ 'CLIPBOARD.ERROR_LOAD' | translate }}</p>
  <button hlmBtn variant="link" size="sm" (click)="clipboard.entries.reload()">
    {{ 'CLIPBOARD.TRY_AGAIN' | translate }}
  </button>
</div>
```

Replace with:
```html
<app-empty-state
  icon="lucideAlertCircle"
  [title]="'CLIPBOARD.ERROR_LOAD' | translate"
  variant="destructive">
  <button hlmBtn variant="link" size="sm" (click)="clipboard.entries.reload()">
    {{ 'CLIPBOARD.TRY_AGAIN' | translate }}
  </button>
</app-empty-state>
```

- [ ] **Step 5: Replace empty state**

Remove:
```html
<div class="flex flex-col items-center justify-center h-full py-10 text-center gap-3">
  <div class="w-10 h-10 rounded-xl bg-card flex items-center justify-center">
    @if (activeTab() === 'pinned') {
      <ng-icon hlm size="base" name="lucideBookmark" class="text-muted-foreground" />
    } @else {
      <ng-icon hlm size="base" name="lucideClipboard" class="text-muted-foreground" />
    }
  </div>
  @if (activeTab() === 'pinned') {
    <p class="text-[13px] text-muted-foreground">{{ 'CLIPBOARD.EMPTY_PINNED' | translate }}</p>
    <p class="text-[11px] text-muted-foreground">{{ 'CLIPBOARD.EMPTY_PINNED_HINT' | translate }}</p>
  } @else if (searchQuery()) {
    <p class="text-[13px] text-muted-foreground">{{ 'CLIPBOARD.EMPTY_NO_MATCHES' | translate:{ term: searchQuery() } }}</p>
  } @else {
    <p class="text-[13px] text-muted-foreground">{{ 'CLIPBOARD.EMPTY_NOTHING' | translate }}</p>
  }
</div>
```

Replace with:
```html
@if (activeTab() === 'pinned') {
  <app-empty-state
    icon="lucideBookmark"
    [title]="'CLIPBOARD.EMPTY_PINNED' | translate"
    [hint]="'CLIPBOARD.EMPTY_PINNED_HINT' | translate"
  />
} @else if (searchQuery()) {
  <app-empty-state
    icon="lucideClipboard"
    [title]="'CLIPBOARD.EMPTY_NO_MATCHES' | translate:{ term: searchQuery() }"
  />
} @else {
  <app-empty-state
    icon="lucideClipboard"
    [title]="'CLIPBOARD.EMPTY_NOTHING' | translate"
  />
}
```

- [ ] **Step 6: Replace kbd hint spans in footer**

Remove these five spans (lines 171–186, 191–193):
```html
<span class="flex items-center gap-1 text-[10px] text-muted-foreground whitespace-nowrap">
  <kbd class="inline-flex items-center px-1 py-0.5 bg-muted border border-border rounded text-[10px] font-mono text-muted-foreground leading-none">↑↓</kbd>
  {{ 'CLIPBOARD.HINT_NAV' | translate }}
</span>
<span class="flex items-center gap-1 text-[10px] text-muted-foreground whitespace-nowrap">
  <kbd class="inline-flex items-center px-1 py-0.5 bg-muted border border-border rounded text-[10px] font-mono text-muted-foreground leading-none">↵</kbd>
  {{ 'CLIPBOARD.HINT_PASTE' | translate }}
</span>
<span class="flex items-center gap-1 text-[10px] text-muted-foreground whitespace-nowrap">
  <kbd class="inline-flex items-center px-1 py-0.5 bg-muted border border-border rounded text-[10px] font-mono text-muted-foreground leading-none">⌫</kbd>
  {{ 'CLIPBOARD.HINT_DELETE' | translate }}
</span>
<span class="flex items-center gap-1 text-[10px] text-muted-foreground whitespace-nowrap">
  <kbd class="inline-flex items-center px-1 py-0.5 bg-muted border border-border rounded text-[10px] font-mono text-muted-foreground leading-none">P</kbd>
  {{ 'CLIPBOARD.HINT_PIN' | translate }}
</span>
...
<span class="flex items-center gap-1 text-[10px] text-muted-foreground whitespace-nowrap">
  <kbd class="inline-flex items-center px-1 py-0.5 bg-muted border border-border rounded text-[10px] font-mono text-muted-foreground leading-none">Esc</kbd>
  {{ 'CLIPBOARD.HINT_CLOSE' | translate }}
</span>
```

Replace with:
```html
<app-keyboard-hint key="↑↓" [label]="'CLIPBOARD.HINT_NAV' | translate" />
<app-keyboard-hint key="↵" [label]="'CLIPBOARD.HINT_PASTE' | translate" />
<app-keyboard-hint key="⌫" [label]="'CLIPBOARD.HINT_DELETE' | translate" />
<app-keyboard-hint key="P" [label]="'CLIPBOARD.HINT_PIN' | translate" />
```

The search hint span (with `ml-auto`) stays inline as-is — it has no `<kbd>` element.

The Esc hint goes at the end:
```html
<app-keyboard-hint key="Esc" [label]="'CLIPBOARD.HINT_CLOSE' | translate" />
```

- [ ] **Step 7: Run tests**

```bash
pnpm test
```

Expected: all PASS

- [ ] **Step 8: Commit**

```bash
git add src/app/features/clipboard-list/clipboard-list.component.ts
git commit -m "refactor(clipboard-list): use PageHeader, EmptyState, KeyboardHint components"
```

---

## Task 9: Refactor settings

**Files:**
- Modify: `src/app/features/settings/settings.component.ts`

Replace header, spinner, `<hr>` separators, field groups, and checkboxes. Simplify event handlers since `SettingCheckboxComponent` emits `boolean` directly instead of `Event`.

- [ ] **Step 1: Update imports**

In `settings.component.ts`, add these imports at the top and in the component `imports` array:

```typescript
import { PageHeaderComponent } from '../../shared/ui/page-header/page-header.component';
import { LoadingSpinnerComponent } from '../../shared/ui/loading-spinner/loading-spinner.component';
import { SettingFieldComponent } from './components/setting-field/setting-field.component';
import { SettingCheckboxComponent } from './components/setting-checkbox/setting-checkbox.component';
import { HlmSeparatorImports } from '@spartan-ng/helm/separator';
```

Add to component `imports` array: `PageHeaderComponent, LoadingSpinnerComponent, SettingFieldComponent, SettingCheckboxComponent, HlmSeparatorImports`

> **Note:** Check `src/libs/ui/separator/src/index.ts` for the exact export name — it may be `HlmSeparatorDirective` or similar. Use whatever is exported.

- [ ] **Step 2: Replace the header div**

Remove:
```html
<div class="px-3.5 h-11 flex items-center gap-2 shrink-0 bg-card border-b border-border" data-tauri-drag-region>
  <a routerLink="/" class="p-1 rounded-md text-muted-foreground hover:text-foreground hover:bg-muted transition-colors">
    <ng-icon hlm size="sm" name="lucideChevronLeft" />
  </a>
  <span class="text-[13px] font-semibold text-foreground tracking-tight">{{ 'SETTINGS.TITLE' | translate }}</span>
</div>
```

Replace with:
```html
<app-page-header>
  <ng-container start>
    <a routerLink="/" class="p-1 rounded-md text-muted-foreground hover:text-foreground hover:bg-muted transition-colors">
      <ng-icon hlm size="sm" name="lucideChevronLeft" />
    </a>
    <span class="text-[13px] font-semibold text-foreground tracking-tight">{{ 'SETTINGS.TITLE' | translate }}</span>
  </ng-container>
</app-page-header>
```

- [ ] **Step 3: Replace the loading spinner**

Remove:
```html
<div class="flex-1 flex items-center justify-center">
  <div class="w-5 h-5 border-2 border-muted border-t-muted-foreground rounded-full animate-spin"></div>
</div>
```

Replace with:
```html
<div class="flex-1 flex items-center justify-center">
  <app-loading-spinner />
</div>
```

- [ ] **Step 4: Replace both `<hr>` separators**

Both occurrences:
```html
<hr class="border-border" />
```

Replace each with (check the exact selector from the generated separator — typically):
```html
<hlm-separator />
```

- [ ] **Step 5: Replace the Shortcut field group**

Remove:
```html
<div class="space-y-1.5">
  <label hlmLabel class="block uppercase tracking-wider">{{ 'SETTINGS.SHORTCUT_LABEL' | translate }}</label>
  <input
    hlmInput
    type="text"
    [value]="settings().shortcut"
    class="w-full font-mono"
    [placeholder]="'SETTINGS.SHORTCUT_PLACEHOLDER' | translate"
    (keydown)="captureShortcut($event)"
    readonly
  />
  <p class="text-[11px] text-muted-foreground">
    {{ 'SETTINGS.SHORTCUT_HINT' | translate }}
  </p>
</div>
```

Replace with:
```html
<app-setting-field
  [label]="'SETTINGS.SHORTCUT_LABEL' | translate"
  [hint]="'SETTINGS.SHORTCUT_HINT' | translate">
  <input
    hlmInput
    type="text"
    [value]="settings().shortcut"
    class="w-full font-mono"
    [placeholder]="'SETTINGS.SHORTCUT_PLACEHOLDER' | translate"
    (keydown)="captureShortcut($event)"
    readonly
  />
</app-setting-field>
```

- [ ] **Step 6: Replace the Autostart field group**

Remove:
```html
<div class="space-y-1.5">
  <div class="flex items-center gap-2">
    <input
      type="checkbox"
      id="autostart-checkbox"
      [checked]="settings().autostart"
      (change)="onAutostartChange($event)"
      class="h-4 w-4 rounded border-border accent-primary cursor-pointer"
    />
    <label hlmLabel for="autostart-checkbox" class="uppercase tracking-wider cursor-pointer">
      {{ 'SETTINGS.AUTOSTART_LABEL' | translate }}
    </label>
  </div>
</div>
```

Replace with:
```html
<app-setting-field>
  <app-setting-checkbox
    id="autostart-checkbox"
    [label]="'SETTINGS.AUTOSTART_LABEL' | translate"
    [checked]="settings().autostart"
    (checkedChange)="onAutostartChange($event)"
  />
</app-setting-field>
```

- [ ] **Step 7: Replace the Window Position field group**

Remove:
```html
<div class="space-y-1.5">
  <label hlmLabel class="block uppercase tracking-wider">{{ 'SETTINGS.WINDOW_POSITION_LABEL' | translate }}</label>
  <div hlmSelect [value]="settings().windowPosition" [itemToString]="windowPositionLabel" (valueChange)="onWindowPositionChange($event)">
    <hlm-select-trigger class="w-full">
      <hlm-select-value />
    </hlm-select-trigger>
    <hlm-select-content *hlmSelectPortal>
      <hlm-select-item value="cursor">{{ 'SETTINGS.WINDOW_POSITION_CURSOR' | translate }}</hlm-select-item>
      <hlm-select-item value="last">{{ 'SETTINGS.WINDOW_POSITION_LAST' | translate }}</hlm-select-item>
    </hlm-select-content>
  </div>
</div>
```

Replace with:
```html
<app-setting-field [label]="'SETTINGS.WINDOW_POSITION_LABEL' | translate">
  <div hlmSelect [value]="settings().windowPosition" [itemToString]="windowPositionLabel" (valueChange)="onWindowPositionChange($event)">
    <hlm-select-trigger class="w-full">
      <hlm-select-value />
    </hlm-select-trigger>
    <hlm-select-content *hlmSelectPortal>
      <hlm-select-item value="cursor">{{ 'SETTINGS.WINDOW_POSITION_CURSOR' | translate }}</hlm-select-item>
      <hlm-select-item value="last">{{ 'SETTINGS.WINDOW_POSITION_LAST' | translate }}</hlm-select-item>
    </hlm-select-content>
  </div>
</app-setting-field>
```

- [ ] **Step 8: Replace the Language field group**

Remove:
```html
<div class="space-y-1.5">
  <label hlmLabel class="block uppercase tracking-wider">{{ 'SETTINGS.LANGUAGE_LABEL' | translate }}</label>
  <div hlmSelect [value]="settings().language ?? ''" [itemToString]="languageLabel" (valueChange)="onLanguageChange($event)">
    <hlm-select-trigger class="w-full">
      <hlm-select-value />
    </hlm-select-trigger>
    <hlm-select-content *hlmSelectPortal>
      <hlm-select-item value="">{{ 'SETTINGS.LANGUAGE_SYSTEM' | translate }}</hlm-select-item>
      <hlm-select-item value="en">{{ 'SETTINGS.LANGUAGE_EN' | translate }}</hlm-select-item>
      <hlm-select-item value="de">{{ 'SETTINGS.LANGUAGE_DE' | translate }}</hlm-select-item>
    </hlm-select-content>
  </div>
</div>
```

Replace with:
```html
<app-setting-field [label]="'SETTINGS.LANGUAGE_LABEL' | translate">
  <div hlmSelect [value]="settings().language ?? ''" [itemToString]="languageLabel" (valueChange)="onLanguageChange($event)">
    <hlm-select-trigger class="w-full">
      <hlm-select-value />
    </hlm-select-trigger>
    <hlm-select-content *hlmSelectPortal>
      <hlm-select-item value="">{{ 'SETTINGS.LANGUAGE_SYSTEM' | translate }}</hlm-select-item>
      <hlm-select-item value="en">{{ 'SETTINGS.LANGUAGE_EN' | translate }}</hlm-select-item>
      <hlm-select-item value="de">{{ 'SETTINGS.LANGUAGE_DE' | translate }}</hlm-select-item>
    </hlm-select-content>
  </div>
</app-setting-field>
```

- [ ] **Step 9: Replace the Theme field group**

Remove:
```html
<div class="space-y-1.5">
  <label hlmLabel class="block uppercase tracking-wider">{{ 'SETTINGS.THEME_LABEL' | translate }}</label>
  <div hlmSelect [value]="settings().theme" [itemToString]="themeLabel" (valueChange)="onThemeChange($event)">
    <hlm-select-trigger class="w-full">
      <hlm-select-value />
    </hlm-select-trigger>
    <hlm-select-content *hlmSelectPortal>
      <hlm-select-item value="system">{{ 'SETTINGS.THEME_SYSTEM' | translate }}</hlm-select-item>
      <hlm-select-item value="light">{{ 'SETTINGS.THEME_LIGHT' | translate }}</hlm-select-item>
      <hlm-select-item value="dark">{{ 'SETTINGS.THEME_DARK' | translate }}</hlm-select-item>
    </hlm-select-content>
  </div>
</div>
```

Replace with:
```html
<app-setting-field [label]="'SETTINGS.THEME_LABEL' | translate">
  <div hlmSelect [value]="settings().theme" [itemToString]="themeLabel" (valueChange)="onThemeChange($event)">
    <hlm-select-trigger class="w-full">
      <hlm-select-value />
    </hlm-select-trigger>
    <hlm-select-content *hlmSelectPortal>
      <hlm-select-item value="system">{{ 'SETTINGS.THEME_SYSTEM' | translate }}</hlm-select-item>
      <hlm-select-item value="light">{{ 'SETTINGS.THEME_LIGHT' | translate }}</hlm-select-item>
      <hlm-select-item value="dark">{{ 'SETTINGS.THEME_DARK' | translate }}</hlm-select-item>
    </hlm-select-content>
  </div>
</app-setting-field>
```

- [ ] **Step 10: Replace the Delete-after-max field group**

Remove:
```html
<div class="space-y-1.5">
  <div class="flex items-center gap-2">
    <input
      type="checkbox"
      id="delete-max-checkbox"
      [checked]="settings().deleteAfterMaxEntries"
      (change)="onDeleteAfterMaxChange($event)"
      class="h-4 w-4 rounded border-border accent-primary cursor-pointer"
    />
    <label hlmLabel for="delete-max-checkbox" class="uppercase tracking-wider cursor-pointer">
      {{ 'SETTINGS.DELETE_AFTER_MAX_LABEL' | translate }}
    </label>
  </div>
  <div class="flex items-center gap-3" [class.opacity-50]="!settings().deleteAfterMaxEntries">
    <input
      hlmInput
      #maxEntriesInput
      type="number"
      [value]="settings().maxEntries"
      (blur)="onMaxEntriesBlur(maxEntriesInput.valueAsNumber)"
      [attr.disabled]="!settings().deleteAfterMaxEntries ? '' : null"
      min="5"
      max="999"
      class="w-24"
    />
    <span class="text-[12px] text-muted-foreground">
      {{ 'SETTINGS.MAX_ENTRIES_RANGE' | translate:{ min: 5, max: 999 } }}
    </span>
  </div>
</div>
```

Replace with:
```html
<app-setting-field>
  <app-setting-checkbox
    id="delete-max-checkbox"
    [label]="'SETTINGS.DELETE_AFTER_MAX_LABEL' | translate"
    [checked]="settings().deleteAfterMaxEntries"
    (checkedChange)="onDeleteAfterMaxChange($event)"
  />
  <div class="flex items-center gap-3" [class.opacity-50]="!settings().deleteAfterMaxEntries">
    <input
      hlmInput
      #maxEntriesInput
      type="number"
      [value]="settings().maxEntries"
      (blur)="onMaxEntriesBlur(maxEntriesInput.valueAsNumber)"
      [attr.disabled]="!settings().deleteAfterMaxEntries ? '' : null"
      min="5"
      max="999"
      class="w-24"
    />
    <span class="text-[12px] text-muted-foreground">
      {{ 'SETTINGS.MAX_ENTRIES_RANGE' | translate:{ min: 5, max: 999 } }}
    </span>
  </div>
</app-setting-field>
```

- [ ] **Step 11: Replace the Delete-after-days field group**

Remove:
```html
<div class="space-y-1.5">
  <div class="flex items-center gap-2">
    <input
      type="checkbox"
      id="delete-days-checkbox"
      [checked]="settings().deleteAfterDays"
      (change)="onDeleteAfterDaysChange($event)"
      class="h-4 w-4 rounded border-border accent-primary cursor-pointer"
    />
    <label hlmLabel for="delete-days-checkbox" class="uppercase tracking-wider cursor-pointer">
      {{ 'SETTINGS.DELETE_AFTER_DAYS_LABEL' | translate }}
    </label>
  </div>
  <div class="flex items-center gap-3" [class.opacity-50]="!settings().deleteAfterDays">
    <input
      hlmInput
      #maxDaysInput
      type="number"
      [value]="settings().maxDays"
      (blur)="onMaxDaysBlur(maxDaysInput.valueAsNumber)"
      [attr.disabled]="!settings().deleteAfterDays ? '' : null"
      min="1"
      max="365"
      class="w-24"
    />
    <span class="text-[12px] text-muted-foreground">
      {{ 'SETTINGS.MAX_DAYS_RANGE' | translate:{ min: 1, max: 365 } }}
    </span>
  </div>
</div>
```

Replace with:
```html
<app-setting-field>
  <app-setting-checkbox
    id="delete-days-checkbox"
    [label]="'SETTINGS.DELETE_AFTER_DAYS_LABEL' | translate"
    [checked]="settings().deleteAfterDays"
    (checkedChange)="onDeleteAfterDaysChange($event)"
  />
  <div class="flex items-center gap-3" [class.opacity-50]="!settings().deleteAfterDays">
    <input
      hlmInput
      #maxDaysInput
      type="number"
      [value]="settings().maxDays"
      (blur)="onMaxDaysBlur(maxDaysInput.valueAsNumber)"
      [attr.disabled]="!settings().deleteAfterDays ? '' : null"
      min="1"
      max="365"
      class="w-24"
    />
    <span class="text-[12px] text-muted-foreground">
      {{ 'SETTINGS.MAX_DAYS_RANGE' | translate:{ min: 1, max: 365 } }}
    </span>
  </div>
</app-setting-field>
```

- [ ] **Step 12: Simplify event handlers in the component class**

The checkbox `(checkedChange)` output emits `boolean` directly, so the `onAutostartChange`, `onDeleteAfterMaxChange`, and `onDeleteAfterDaysChange` methods no longer need to unwrap an `Event`. Update them:

```typescript
protected onAutostartChange(checked: boolean): void {
  this.settings.update(s => ({ ...s, autostart: checked }));
  this.persist();
}

protected onDeleteAfterMaxChange(checked: boolean): void {
  this.settings.update(s => ({ ...s, deleteAfterMaxEntries: checked }));
  this.persist();
}

protected onDeleteAfterDaysChange(checked: boolean): void {
  this.settings.update(s => ({ ...s, deleteAfterDays: checked }));
  this.persist();
}
```

Also remove `HlmLabel` from the `imports` array — it's now only used inside `SettingFieldComponent` and `SettingCheckboxComponent`.

- [ ] **Step 13: Run tests**

```bash
pnpm test
```

Expected: all PASS

- [ ] **Step 14: Commit**

```bash
git add src/app/features/settings/settings.component.ts
git commit -m "refactor(settings): use PageHeader, LoadingSpinner, HlmSeparator, SettingField, SettingCheckbox"
```

---

## Task 10: Refactor image-preview

**Files:**
- Modify: `src/app/features/image-preview/image-preview.component.ts`

- [ ] **Step 1: Update imports**

Add to top of file:
```typescript
import { PageHeaderComponent } from '../../shared/ui/page-header/page-header.component';
import { LoadingSpinnerComponent } from '../../shared/ui/loading-spinner/loading-spinner.component';
import { EmptyStateComponent } from '../../shared/ui/empty-state/empty-state.component';
```

Add to component `imports` array: `PageHeaderComponent, LoadingSpinnerComponent, EmptyStateComponent`

Remove from `providers`: `lucideAlertCircle` (now registered inside EmptyStateComponent). Keep: `lucideLoader, lucideCopy, lucideX, lucideCheck`.

- [ ] **Step 2: Replace the toolbar**

Remove:
```html
<div class="px-3.5 h-11 flex items-center justify-between shrink-0 bg-zinc-900 border-b border-zinc-800">
  <span class="text-[13px] font-semibold text-zinc-200 tracking-tight">{{ 'IMAGE_PREVIEW.TITLE' | translate }}</span>
  <div class="flex items-center gap-2">
    <button hlmBtn variant="outline" size="sm" class="flex items-center gap-1.5" [disabled]="copying()" (click)="copyToClipboard()">
      @if (copying()) {
        <ng-icon hlm size="sm" name="lucideLoader" class="animate-spin" />
        {{ 'IMAGE_PREVIEW.COPYING' | translate }}
      } @else {
        <ng-icon hlm size="sm" name="lucideCopy" />
        {{ 'IMAGE_PREVIEW.COPY' | translate }}
      }
    </button>
    <button hlmBtn variant="ghost" size="sm" class="w-8 h-8 p-0 flex items-center justify-center" (click)="closeWindow()" [title]="'IMAGE_PREVIEW.CLOSE' | translate">
      <ng-icon hlm size="sm" name="lucideX" />
    </button>
  </div>
</div>
```

Replace with:
```html
<app-page-header variant="dark" [dragRegion]="false">
  <ng-container start>
    <span class="text-[13px] font-semibold text-zinc-200 tracking-tight">{{ 'IMAGE_PREVIEW.TITLE' | translate }}</span>
  </ng-container>
  <ng-container end>
    <button hlmBtn variant="outline" size="sm" class="flex items-center gap-1.5" [disabled]="copying()" (click)="copyToClipboard()">
      @if (copying()) {
        <ng-icon hlm size="sm" name="lucideLoader" class="animate-spin" />
        {{ 'IMAGE_PREVIEW.COPYING' | translate }}
      } @else {
        <ng-icon hlm size="sm" name="lucideCopy" />
        {{ 'IMAGE_PREVIEW.COPY' | translate }}
      }
    </button>
    <button hlmBtn variant="ghost" size="sm" class="w-8 h-8 p-0 flex items-center justify-center" (click)="closeWindow()" [title]="'IMAGE_PREVIEW.CLOSE' | translate">
      <ng-icon hlm size="sm" name="lucideX" />
    </button>
  </ng-container>
</app-page-header>
```

- [ ] **Step 3: Replace the loading spinner**

Remove:
```html
<div class="flex flex-col items-center gap-3">
  <div class="w-5 h-5 border-2 border-zinc-800 border-t-zinc-500 rounded-full animate-spin"></div>
  <span class="text-[13px] text-zinc-500">{{ 'IMAGE_PREVIEW.LOADING' | translate }}</span>
</div>
```

Replace with:
```html
<div class="flex flex-col items-center gap-3">
  <app-loading-spinner variant="dark" />
  <span class="text-[13px] text-zinc-500">{{ 'IMAGE_PREVIEW.LOADING' | translate }}</span>
</div>
```

- [ ] **Step 4: Replace the error state**

Remove:
```html
<div class="flex flex-col items-center gap-3">
  <div class="w-9 h-9 rounded-full bg-red-500/10 flex items-center justify-center">
    <ng-icon hlm size="sm" name="lucideAlertCircle" class="text-red-400" />
  </div>
  <p class="text-[13px] text-zinc-400">{{ 'IMAGE_PREVIEW.ERROR' | translate }}</p>
</div>
```

Replace with:
```html
<app-empty-state
  icon="lucideAlertCircle"
  [title]="'IMAGE_PREVIEW.ERROR' | translate"
  variant="destructive"
/>
```

- [ ] **Step 5: Run tests**

```bash
pnpm test
```

Expected: all PASS

- [ ] **Step 6: Commit**

```bash
git add src/app/features/image-preview/image-preview.component.ts
git commit -m "refactor(image-preview): use PageHeader, LoadingSpinner, EmptyState components"
```

---

## Self-Review Notes

- **Spec coverage:** All 8 components from the spec are implemented (Tasks 2–7 + Task 1 for spartan). All three consumer refactors covered (Tasks 8–10).
- **Type consistency:** `output<boolean>()` used in SettingCheckboxComponent matches the `boolean` parameter in the simplified handlers in Task 9 Step 12.
- **Placeholder check:** Task 7 Step 4 notes that the HlmCheckbox import name must be verified from the generated file — this is a genuine runtime unknown, not a lazy placeholder.
- **`HlmLabel` removal from settings:** Step 12 notes to remove `HlmLabel` from the settings component imports array since it's now encapsulated in the child components. Don't forget this or the build will have an unused import (not a bug, but untidy).
